services:
  jellyfin:
    image: lscr.io/linuxserver/jellyfin:latest
    environment:
      - PUID=${HL_USER}
      - PGID=${HL_GROUP}
      - TZ=${HL_TZ}
    volumes:
      - ${HL_CONFIG_FOLDER}/Jellyfin:/config
      - ${HL_DATA_FOLDER}/TV:/data/tvshows
      - ${HL_DATA_FOLDER}/Movies:/data/movies
      - ${HL_DATA_FOLDER}/Videos:/data/videos
      - ${HL_DATA_FOLDER}/Photos:/data/photos
      - ${HL_DATA_FOLDER}/Music:/data/music
    ports:
      - 8096:8096 # Web UI (HTTP)
      - 7359:7359/udp # Auto-discovery (LAN)
      - 1900:1900/udp # DLNA
    restart: unless-stopped
    networks:
      - main

networks:
  main:
    name: main
    external: true
